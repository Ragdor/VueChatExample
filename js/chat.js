define(['../libs/vue',
        './store',
        './components/ChatWindow'], 
function(Vue, Store) {
    new Vue({
        el: '#chat',
        data: {
        },
        store: Store,
        methods: {
        }
      })
});
